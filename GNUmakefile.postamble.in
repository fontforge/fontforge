# -*- Makefile -*-
#
# Support for --with-gnustep-make

before-all::

after-all::

after-clean::

after-distclean::
	rm -f GNUmakefile GNUmakefile.postamble overall_common.mk
	rm -f Makefile
	rm -f libltdl/GNUmakefile libltdl/Makefile
	rm -f fontforge.pc
	rm -f inc/config.h
	rm -f config.log config.cache
	rm -f config.status
	rm -f tests/atconfig tests/atlocal tests/*.py
	rm -rf Unicode/.deps gutils/.deps gdraw/.deps fontforge/.deps

after-clean::
