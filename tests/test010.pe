#Needs: fonts/GillSans.dfont
#Print("....I expect this test to generate some error messages");
#Print(".... But it should not crash, nor terminate with an error status");
Open($1);
SelectAll();
Simplify();
Print("....Simplified");
ExpandStroke(20);
Print("....Expanded");
Scale(200,150);
Print("....Scaled");
#RemoveOverlap();
#Print("....Simple Remove Overlap test");
Quit()
